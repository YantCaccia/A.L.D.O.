import ApiSubset from '../ApiSubset';
import { AudioTypes, FFIFunctionCall, HermesOptions } from '../types';
/**
 * @experimental
 *
 * Warning: Experimental, use at your own risk!
 *
 * The Audio API subset.
 */
export default class Audio extends ApiSubset {
    constructor(protocolHandler: Buffer, call: FFIFunctionCall, options: HermesOptions);
    publishEvents: {
        play_audio: {
            fullEventName: string;
        };
    };
    publishMessagesList: AudioTypes.publishMessagesList;
    subscribeEvents: {
        'play_finished/': {
            fullEventName: string;
            additionalArguments: (eventName: any) => Buffer[];
        };
        play_finished_all: {
            fullEventName: string;
        };
    };
    subscribeMessagesList: AudioTypes.subscribeMessagesList;
    destroy(): void;
}
