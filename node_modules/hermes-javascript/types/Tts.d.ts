import { RegisterSoundMessage } from './messages';
export declare namespace TtsTypes {
    /**
     * The name and type of message that the Tts subset can publish.
     */
    type publishMessagesList = {
        register_sound: RegisterSoundMessage;
    };
}
